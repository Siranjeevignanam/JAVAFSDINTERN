spring:
  application:
    name: SpringBootMicroserviceDiscovery
#eureka discovery server configuration

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 9898